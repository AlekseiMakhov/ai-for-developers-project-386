name: CD

on:
  push:
    branches: [main]

jobs:
  deploy-api:
    name: Deploy API to Railway
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Railway redeploy
        run: curl -f "${{ secrets.RAILWAY_DEPLOY_HOOK }}"
